# FastAPI backend dependencies
# Install alongside existing backend/requirements.txt deps (numpy, opencv, torch, etc.)
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
aiofiles>=23.2.1
pydantic-settings>=2.0.0
